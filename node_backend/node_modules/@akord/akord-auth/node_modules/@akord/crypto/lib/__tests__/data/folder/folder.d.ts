import { EncryptedKeys } from "../../../encrypter";
import { Encryptable } from "../../../decorators";
export declare class Folder extends Encryptable {
    name: string;
    id: string;
    constructor(name: string, keys: Array<EncryptedKeys>, publicKey?: string);
}
