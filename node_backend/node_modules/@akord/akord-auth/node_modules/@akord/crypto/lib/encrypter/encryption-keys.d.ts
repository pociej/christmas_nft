import { Encrypted, EncryptedKeys } from './types';
export declare class EncryptionKeys extends Encrypted {
    publicKeys: string[];
    constructor(keys?: EncryptedKeys[], publicKeys?: string[]);
    getPublicKey(): Uint8Array;
    setPublicKey(publicKey: string): void;
    setPublicKeys(publicKeys: string[]): void;
}
