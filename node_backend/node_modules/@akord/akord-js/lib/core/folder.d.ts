import { NodeCreateOptions, NodeService } from "./node";
import { Folder, nodeType } from "../types/node";
declare class FolderService extends NodeService<Folder> {
    objectType: nodeType;
    NodeType: typeof Folder;
    /**
     * @param  {string} vaultId
     * @param  {string} name folder name
     * @param  {NodeCreateOptions} [options] parent id, etc.
     * @returns Promise with new folder id & corresponding transaction id
     */
    create(vaultId: string, name: string, options?: NodeCreateOptions): Promise<FolderCreateResult>;
}
declare type FolderCreateResult = {
    folderId: string;
    transactionId: string;
    object: Folder;
};
export { FolderService };
