import { ListOptions } from "../types/query-options";
import { EncryptionMetadata } from "./service";
import { EncryptedPayload } from "@akord/crypto/lib/types";
export declare const handleListErrors: <T>(originalItems: T[], promises: Promise<T>[]) => Promise<{
    items: T[];
    errors: Array<{
        id: string;
        error: Error;
    }>;
}>;
export declare const paginate: <T>(apiCall: any, listOptions: ListOptions & {
    vaultId?: string;
}) => Promise<T[]>;
export declare const mergeState: (currentState: any, stateUpdates: any) => any;
export declare const getEncryptedPayload: (data: ArrayBuffer | string, metadata: EncryptionMetadata) => EncryptedPayload;
