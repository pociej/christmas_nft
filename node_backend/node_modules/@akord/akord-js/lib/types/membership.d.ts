import { Encryptable, EncryptedKeys } from "@akord/crypto";
import { ProfileDetails } from "./profile-details";
export declare type RoleType = "VIEWER" | "CONTRIBUTOR" | "OWNER";
export declare type StatusType = "ACCEPTED" | "PENDING" | "REVOKED" | "INVITED";
export declare class Membership extends Encryptable {
    id: string;
    owner: string;
    createdAt: string;
    updatedAt: string;
    expiresAt: string;
    status: StatusType;
    address: string;
    role: RoleType;
    data?: string[];
    encPublicSigningKey: string;
    email: string;
    memberPublicSigningKey: string;
    memberDetails: ProfileDetails;
    vaultId: string;
    keys: EncryptedKeys[];
    __public__?: boolean;
    __cacheOnly__?: boolean;
    constructor(membershipProto: any, keys?: Array<EncryptedKeys>);
}
export declare type MembershipKeys = {
    isEncrypted: boolean;
    keys: EncryptedKeys[];
    publicKey?: string;
};
