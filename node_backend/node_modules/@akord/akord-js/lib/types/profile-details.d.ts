import { Encryptable, EncryptedKeys } from "@akord/crypto";
export declare class ProfileDetails extends Encryptable {
    constructor(profileDetailsProto: any, keys?: Array<EncryptedKeys>, publicKey?: string);
    name?: string;
    avatarUri?: string[];
    avatar?: ArrayBuffer;
}
