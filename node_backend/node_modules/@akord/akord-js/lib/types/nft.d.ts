import { AssetMetadata } from "./asset";
import { FileVersion, Node, StorageType } from "./node";
export declare class NFT extends Node {
    ticker: string;
    name: string;
    description: string;
    collection: string;
    creator: string;
    owner: string;
    balances: {
        [x: string]: number;
    };
    claimable: Claim[];
    asset: FileVersion;
    constructor(nodeLike: any);
    getUri(type?: StorageType): string;
}
export declare type Claim = {
    from: string;
    qty: number;
    to: string;
    txID: string;
};
export declare type NFTMetadata = {
    owner: string;
    creator?: string;
    collection?: string;
    contractTxId?: string;
    ticker?: string;
} & AssetMetadata;
