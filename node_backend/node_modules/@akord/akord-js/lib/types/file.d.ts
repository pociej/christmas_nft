/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
import { Blob } from "buffer";
import { BinaryLike } from "crypto";
import { Readable } from "stream";
export declare namespace NodeJs {
    class File extends Blob {
        name: string;
        lastModified: number;
        constructor(sources: Array<BinaryLike | Blob>, name: string, mimeType?: string, lastModified?: number);
        static fromReadable(stream: Readable, name: string, mimeType?: string, lastModified?: number): Promise<File>;
        static fromPath(filePath: string): Promise<File>;
    }
}
export declare type FileLike = NodeJs.File | File;
export declare type FileSource = FileLike | ArrayBuffer | Buffer | string | Readable | Array<BinaryLike | any>;
