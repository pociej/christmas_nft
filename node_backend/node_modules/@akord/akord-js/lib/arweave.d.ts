import { Tags } from "./types/contract";
declare const getTxData: (id: string, responseType?: string) => Promise<any>;
declare const getTxMetadata: (id: string) => Promise<TransactionMetadata>;
export declare type TransactionMetadata = {
    id: string;
    owner: {
        address: string;
    };
    data: {
        type: string;
        size: string;
    };
    tags: Tags;
    block: {
        timestamp: number;
    };
};
export { getTxData, getTxMetadata };
